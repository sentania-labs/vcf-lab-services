name: vcf-services

services:
  depot-web:
    container_name: vcf-services-depot-web
    image: caddy:2.10.0-alpine
    restart: unless-stopped
    init: true
    env_file:
      - secrets/caddy.env
    ports:
      - "${HTTPS_PORT:-443}:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - depot_store:/depot:ro
      - ./secrets/tls:/certs:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - control
    depends_on:
      admin-ui:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q --no-check-certificate -O /dev/null https://127.0.0.1/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s

  depot-sync:
    container_name: vcf-services-sync
    image: ${VCF_SERVICES_SYNC_IMAGE:-vcf-services-sync-base:local}
    restart: unless-stopped
    environment:
      SETTINGS_FILE: /etc/vcf-services/settings.env
      STATE_DIR: /state
      DEPOT_DIR: /depot
      AUTH_FILE: /run/secrets/activation-code.txt
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_PASSWORD_FILE: /run/secrets/redis/password
      TOOL_ROOT: /opt/vcfdt/current
      VCFDT_TOOL_STORE: /opt/vcfdt
    volumes:
      - depot_store:/depot:rw
      - vcfdt_tool:/opt/vcfdt:rw
      - vcfdt_state:/root/.local/share/vmware/vdt
      - sync_state:/state:rw
      - ./config:/etc/vcf-services:ro
      - ./secrets:/run/secrets:ro
    networks:
      - control
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "test -s /state/state.json && kill -0 1"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s

  sftp-backup:
    container_name: vcf-services-sftp
    image: ${VCF_SERVICES_SFTP_IMAGE:-vcf-services-sftp:local}
    restart: unless-stopped
    init: true
    environment:
      SETTINGS_FILE: /config/settings.env
      SFTP_PASSWORD_FILE: /run/sftp-secrets/password
    ports:
      - "${SFTP_PORT:-2222}:22"
    volumes:
      - backup_store:/mnt/backup:rw
      - sftp_host_keys:/etc/ssh/keys:rw
      - ./config:/config:ro
      - ./secrets/sftp:/run/sftp-secrets:ro
    networks:
      - control
    healthcheck:
      test: ["CMD", "/usr/local/bin/sftp-healthcheck.sh"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s

  admin-ui:
    container_name: vcf-services-ui
    image: ${VCF_SERVICES_UI_IMAGE:-vcf-services-ui:local}
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
      depot-sync:
        condition: service_healthy
    environment:
      DEPOT_DIR: /depot
      STATE_DIR: /state
      SETTINGS_FILE: /config/settings.env
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_PASSWORD_FILE: /run/redis/password
      VCFDT_STORE: /opt/vcfdt
    volumes:
      - depot_store:/depot:ro
      - vcfdt_tool:/opt/vcfdt:rw
      - sync_state:/state:ro
      - ./config:/config
      - ./secrets/redis:/run/redis:ro
      - ./secrets/sftp:/run/sftp-secrets
    networks:
      - control
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s

  redis:
    container_name: vcf-services-redis
    image: redis:7.4-alpine
    restart: unless-stopped
    command: ["sh", "-c", "exec redis-server /etc/redis/redis.conf"]
    volumes:
      - ./secrets/redis:/etc/redis:ro
    networks:
      - control
    healthcheck:
      test: ["CMD-SHELL", "REDISCLI_AUTH=\"$$(cat /etc/redis/password)\" redis-cli ping | grep -q PONG"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 5s

networks:
  control:
    driver: bridge

volumes:
  depot_store:
    name: ${DEPOT_VOLUME_NAME:-vcf-services-depot-store}
    driver: local
    driver_opts:
      type: ${DEPOT_VOLUME_TYPE:-none}
      o: ${DEPOT_VOLUME_OPTIONS:-bind}
      device: ${DEPOT_VOLUME_DEVICE:-/var/lib/vcf-services/depot}
  backup_store:
    name: ${BACKUP_VOLUME_NAME:-vcf-services-backup-store}
    driver: local
    driver_opts:
      type: ${BACKUP_VOLUME_TYPE:-none}
      o: ${BACKUP_VOLUME_OPTIONS:-bind}
      device: ${BACKUP_VOLUME_DEVICE:-/var/lib/vcf-services/backup}
  vcfdt_state:
    name: vcf-services-vcfdt-state
    external: true
  vcfdt_tool:
    name: vcf-services-vcfdt-tool
  sync_state:
    name: vcf-services-sync-state
  caddy_data:
    name: vcf-services-caddy-data
  caddy_config:
    name: vcf-services-caddy-config
  sftp_host_keys:
    name: vcf-services-sftp-host-keys
    external: true
